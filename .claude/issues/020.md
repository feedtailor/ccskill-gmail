---
id: 020
task: uninstall.sh の実装（スキルアンインストールスクリプト）
date: 2026-01-26 14:30:00
status: 完了
phase: 4
---

## タスク概要

ccskill-gmail をプロジェクトからアンインストールする `uninstall.sh` スクリプトを実装する。

## 機能要件

- .ccskill-gmail/ ディレクトリの削除
- .claude/skills/ccskill-gmail/ の削除
- .env からの GMAIL_ENDPOINT 削除
- GAS プロジェクトの削除（オプション、確認付き）

## 参考実装

ccskill-spreadsheet/uninstall.sh を参考にする。

## 実装内容

```bash
#!/bin/bash
# uninstall.sh - ccskill-gmail アンインストールスクリプト

# 主な処理:
# 1. アンインストール確認プロンプト
# 2. .ccskill-gmail/ ディレクトリを削除
# 3. .claude/skills/ccskill-gmail/ を削除
# 4. .env から GMAIL_ENDPOINT を削除
# 5. GAS プロジェクトの削除（オプション）
# 6. 完了メッセージ
```

## 注意事項

- 削除前に必ず確認プロンプトを表示
- GAS プロジェクト削除は別途確認（データ損失のリスク）
- .env の他の設定は維持

## テスト方法

```bash
# テスト環境でアンインストールを実行
cd /tmp/test-gmail-skill
/path/to/ccskill-gmail/uninstall.sh
```

## 依存関係

なし（Phase 3 完了後に実装推奨）

# 作業記録

## 2026-01-26

- ccskill-spreadsheet/uninstall.sh を参考に uninstall.sh を作成
- .ccskill-gmail/ ディレクトリの削除
- .claude/skills/ccskill-gmail/ の削除
- .env からの GMAIL_ENDPOINT 削除
- GAS プロジェクト削除のガイダンス表示
