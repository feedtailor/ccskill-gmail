---
id: 132
task: "新規ユーザーの first-run dead-end を解消（setup/release の案内を account add → skill install に）"
date: 2026-06-16
status: 未着手
depends_on: []
blocks: []
---

## 概要

`setup` 完了後・`release`（配布）後の「次の一手」案内が、新規ユーザーに `ccskill-gmail install` を**唯一の次手順**として提示する。しかし共有セットアップの install は account 未登録だと abort する（`install.sh:148-157`「No account registered yet」）。README の正規導線は `account add` → `skill install`。新規ユーザーが**最初の画面で行き止まる**。

## 多エージェント議論の裁定 (2026-06-16)

指摘 C は **KEEP**。ただし当初の「10 箇所・一律 中」は誇張で、**実質 3 点**に絞る。多くの文言（`provision.sh:37-38`/`156-160`、`info.sh:69-75`、`install.sh:493`、`doctor.sh:273-279`、`ccskill-gmail:35`）は**文脈上正しく**、除外する（懐疑側の指摘を擁護側も concede）。

## 確定した事実（コード根拠）

- `setup.sh:130-134` と `release.sh:63-67` が `ccskill-gmail install` を唯一の next step として提示。
- central install は新規ユーザーで `install.sh:148-157` で abort。README 正規導線は `account add` → `skill install`。
- 正しい文言は既にリポジトリ内に存在（`install.sh:225-235` は `ccskill-gmail api whoami`/`api get`、`doctor.sh:183` は `install (or: account add)`）→ 修正コストは低い。

## 実装内容（3 点に絞る）

1. **（主・要対応）** `setup.sh:130-134` と `release.sh:63-67` の案内を README の正規導線 `ccskill-gmail account add` → `ccskill-gmail skill install` に修正。プロジェクト固定の `install` は account 登録後の任意手順として後段に。
2. **（低）** `ccskill-gmail:42` のトップレベルヘルプで `--user NAME` の露出を削減（`--dedicated` 配下に記すか省略）。README は `--account` を案内しており、`--user` は dedicated/legacy モードを強制する（`install.sh:97-100`）。
3. **（瑣末）** `doctor.sh:190/277/282` の install-only な Fix hint を、`doctor.sh:183` の `(or: account add)` 形式に揃える。

**除外（文脈上正しい）**: `provision.sh:37-38`・`provision.sh:156-160`・`info.sh:69-75`・`install.sh:493`・`doctor.sh:273-279`・`ccskill-gmail:35`。

## 完了条件

- [ ] `setup` / `release` の案内が `account add` → `skill install` を提示し、新規ユーザーが dead-end しない
- [ ] `--user` のトップレベル露出を整理
- [ ] `doctor` の Fix hint を整合

## 関連

- 監査の裁定: **C = KEEP（scope を 3 点に縮小）**
- #131 / #133 / #134
