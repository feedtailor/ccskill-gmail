---
id: 017
task: Phase 3 スキル定義の更新
date: 2026-01-26 14:00:00
status: 完了
---

## タスク概要

Phase 3 で追加した API のスキル定義（SKILL.md, reference/）を更新する。

## 更新内容

### 1. SKILL.md の API 一覧に追加

```markdown
### 読み取り (GET) - Phase 3

| アクション | 説明 | パラメータ |
|-----------|------|-----------||
| get_unread_count | 未読メール数取得 | `label` (任意、デフォルト INBOX) |

### 書き込み (POST) - Phase 3

| アクション | 説明 | パラメータ |
|-----------|------|-----------|
| archive | アーカイブ | `threadId` |
| move_to_trash | ゴミ箱に移動 | `threadId` |
| update_draft | 下書き更新 | `draftId`, `to`, `subject`, `body`, `cc`, `bcc` |
| delete_draft | 下書き削除 | `draftId` |
```

### 2. reference/ に追加

- reference/thread.md を新規作成（archive, move_to_trash）
- reference/read.md に `get_unread_count` を追記
- reference/draft.md に `update_draft`, `delete_draft` を追記
- reference/index.md を更新

### 3. examples.md に追加

Phase 3 API を使ったワークフロー例：
- メール処理完全ワークフロー（確認→返信→既読→ラベル→アーカイブ）
- 未読数の監視
- 下書きの管理

### 4. curl テンプレートを追加

```bash
# 未読数取得
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_unread_count"

# アーカイブ
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data '{"action":"archive","threadId":"THREAD_ID"}' \
  "$GMAIL_ENDPOINT"

# ゴミ箱に移動
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data '{"action":"move_to_trash","threadId":"THREAD_ID"}' \
  "$GMAIL_ENDPOINT"

# 下書き削除
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data '{"action":"delete_draft","draftId":"DRAFT_ID"}' \
  "$GMAIL_ENDPOINT"
```

## 依存関係

013, 014, 015, 016 の完了後

# 作業記録

## 2026-01-26

- SKILL.md の API 一覧と curl テンプレートを更新
- reference/index.md を更新
- reference/thread.md を新規作成（archive, move_to_trash）
- reference/read.md に get_unread_count を追加
- reference/draft.md に update_draft / delete_draft を追加
- examples.md に Phase 3 ワークフロー例を追加（#11-14）
