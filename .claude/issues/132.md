---
id: 132
task: "新規ユーザーが最初の操作で行き止まる問題の解消（setup/release の案内を account add → skill install に）"
date: 2026-06-16
status: 完了
depends_on: []
blocks: []
---

## 概要

`setup` を終えた後や `release`（配布）の後に表示する「次の一手」が、新しいユーザーに `ccskill-gmail install` だけを案内する。しかしユーザレベルのセットアップの install は、アカウント未登録だと途中で止まる（`install.sh:148-157`「No account registered yet」）。README が案内する正しい順番は `account add` → `skill install` で、案内とコードが食い違っている。新しいユーザーが最初の画面で行き止まる。

## 多エージェント議論の裁定 (2026-06-16)

指摘 C は対応指摘として残す（裁定は KEEP）。ただし当初の「10 箇所・一律で中程度」は広げすぎで、実際に直すべきは 3 点に絞る。残りの文言（`provision.sh:37-38` / `156-160`、`info.sh:69-75`、`install.sh:493`、`doctor.sh:273-279`、`ccskill-gmail:35`）は、その文脈では正しい案内なので対象から外す（懐疑側の指摘を擁護側も認めた）。

## 確定した事実（コード根拠）

- `setup.sh:130-134` と `release.sh:63-67` が `ccskill-gmail install` を唯一の次の手順として案内する。
- ユーザレベルのセットアップの install は、新規ユーザーで `install.sh:148-157` に達して止まる。README の正しい順番は `account add` → `skill install`。
- 正しい文言は既にリポジトリ内にある（`install.sh:225-235` は `ccskill-gmail api whoami` / `api get`、`doctor.sh:183` は `install (or: account add)`）。直すコストは小さい。

## 実装内容（3 点に絞る）

1. （主・要対応）`setup.sh:130-134` と `release.sh:63-67` の案内を、README の正しい順番 `ccskill-gmail account add` → `ccskill-gmail skill install` に直す。プロジェクトを特定のアカウントに固定する `install` は、アカウント登録後の任意の手順として後ろに置く。
2. （小）`ccskill-gmail:42` のヘルプで `--user NAME` の表示を控える（`--dedicated` の下に記すか省く）。README は `--account` を案内しており、`--user` は専用 GAS（レガシー）の構成を強制する（`install.sh:97-100`）。
3. （ごく小）`doctor.sh:190/277/282` の install だけを示す Fix 案内を、`doctor.sh:183` の `(or: account add)` の形に合わせる。

対象から外す（その文脈では正しい）: `provision.sh:37-38`・`provision.sh:156-160`・`info.sh:69-75`・`install.sh:493`・`doctor.sh:273-279`・`ccskill-gmail:35`。

## 完了条件

- [ ] `setup` / `release` の案内が `account add` → `skill install` を示し、新規ユーザーが行き止まらない
- [ ] `--user` のヘルプ表示を整理する
- [ ] `doctor` の Fix 案内を揃える

## 関連

- 監査の裁定: C = KEEP（対象を 3 点に縮小）
- #131 / #133 / #134
