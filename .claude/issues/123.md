---
id: 123
task: "Phase 2: 中央アカウントレジストリ + account コマンド"
date: 2026-06-10
status: 完了
phase: 2
depends_on: [122]
blocks: [124]
parallel_group: global-multiaccount
---

## 概要

`~/.ccskill-gmail/accounts.json` による複数アカウントの中央登録と、`account` コマンド群・`--account` フラグを実装する。この Phase の完了で「install なしでも `account add` 1 回で全ディレクトリから利用可能」が CLI レベルで成立する。スキーマ・解決優先順位の詳細は #121 を参照。

## 実装内容

1. **`lib/accounts.sh` (新規)** — accounts.json の CRUD・アカウント解決ロジック。`lib/registry.sh` の実装パターン (jq + atomic write) を踏襲。ディレクトリ 700 / ファイル 600
2. **`lib/provision.sh` (新規)** — `commands/install.sh:262-435` の GAS プロビジョニング部 (clasp create → push → deploy → ブラウザ認可 → 検証) を関数化して抽出。install.sh は抽出後の関数を呼ぶ形に書き換え (挙動不変)
3. **`commands/account.sh` (新規)**
   - `account add [--label NAME]` — clasp login (--user は email から自動生成) → provision (GAS タイトル: `Gmail Skill - <email>`) → get_profile で email 確定 → accounts.json 登録。登録 1 件目は自動的に default_account
   - `account list` / `account default <email|label>` / `account remove <email>` (GAS は残す旨を案内)
4. **`lib/api` のアカウント解決チェーン完成** — #122 の解決順に続けて: `--account` フラグ (先頭のみ許可) > `CCSKILL_GMAIL_ACCOUNT` 環境変数 > (cwd メタデータ) > default_account > 登録 1 件ならそれ > `NO_ACCOUNT` エラー
5. **レスポンスへのアカウント注入** — ディスパッチ後に jq で `+ {"account": "<email>", "account_source": "flag|binding|default"}` をマージ (jq 不在時はスキップ)
6. **`api whoami`** — 解決されるアカウントと解決根拠を返す
7. **`lib/auth.sh` 小改修** — トークンリフレッシュ書き戻し失敗時もリフレッシュ済みトークンで処理続行できることを明示的に保証 (sandbox 内実行への縮退対応)
8. **`info` / `status` / `doctor` の対応** — アカウント解決結果の表示、accounts 一覧、accounts.json の診断

## 完了条件

- [x] `account add` 1 回 (ブラウザ認可 1 回) で、install していない任意のディレクトリから `ccskill-gmail api get action=get_profile` が動く (2026-06-10 実機 E2E: `account add --label main` → /tmp で whoami が default 解決、マスター直下で get_profile が ok:true + account 注入)
- [x] 2 アカウント登録時、`--account` で切り替えられ、未指定時は default_account が使われる (ユニットテストで固定)
- [x] レスポンスに `account` / `account_source` が含まれる (accounts.json 解決時のみ注入。バインド/レガシー経路はバイト無加工)
- [x] レガシー install 済みプロジェクトでは従来アカウントが優先される (ユニットテスト + ftgmail 実機で確認)
- [x] `account remove` 後の参照が `NO_ACCOUNT` エラーになる (ユニットテストで固定)

## 実装記録 (2026-06-10)

- `lib/accounts.sh` 新規: accounts.json CRUD (init/upsert/get/resolve_default/set_default/remove/list/count)。registry.sh の jq + atomic write パターン踏襲、700/600。HOME を呼び出し時解決にしてテスト容易性を確保
- `lib/provision.sh` 新規: install.sh:262-435 の GAS プロビジョニングを verbatim 抽出 (`provision_gas`)。再実行ヒントは PROVISION_RETRY_HINT / PROVISION_VERIFY_HINT で差し替え可能。install.sh は呼び出しに置換 (挙動不変、Step 表示の文言のみ簡素化)
- `commands/account.sh` 新規: add (--label / --user、clasp user 衝突時は連番) / list / default / remove (--yes)。GAS ディレクトリは `~/.ccskill-gmail/gas/<clasp_user>/`、タイトルは `Gmail Skill - account <label|clasp_user>` (email は認可前に未確定のため)
- `lib/api`: `--account` フラグ (先頭のみ、`--account=x` 形式も可) / `CCSKILL_GMAIL_ACCOUNT` / accounts.json default / single / NO_ACCOUNT の解決チェーン、`whoami` サブコマンド (バインド時は .registry.json から email をベストエフォート解決)、レスポンス注入。出力は tee からファイル経由に変更 (注入のため。バイト等価性はテストで担保)
- `lib/auth.sh`: トークン書き戻しを RC_FILE 隣接 tmp + ブレース 2>/dev/null に変更。sandbox で HOME に書けない場合も無音でリフレッシュ済みトークンで続行 (検証事項 (2) の帰結)
- `lib/history.sh`: 追記失敗 (sandbox) を無言スキップに (mkdir 失敗時と同じ縮退)
- `commands/doctor.sh`: accounts.json 診断 (妥当性 / default 整合)、install なし + アカウント登録ありをエラーにしない
- 監査ログ: メタデータ無し解決 (accounts.json / GMAIL_ENDPOINT) では記録しない。実装中にマスター lib/ へ audit.jsonl が生成される穴を検出し修正 (テストで固定)。アカウント別の中央集約 (`~/.ccskill-gmail/history/<email>/`) は #125 で対応
- テスト: `tests/lib/test-accounts.sh` (13)、`tests/lib/test-api-accounts.sh` (13) 新規。既存 41 件含め全 67 件 green。ftgmail 実機でレガシー/グローバル/whoami の 3 経路確認

### スコープ調整

- `info` / `status` のアカウント表示対応は #125 (doctor 拡充とまとめて) に送る。`account list` が一覧表示を担うため Phase 2 の必須機能は満たしている

## 関連

- #121: 全体方針 (スキーマ・優先順位の正本)
- #122: 前 Phase / #124: 次 Phase
