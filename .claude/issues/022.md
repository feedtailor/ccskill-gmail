---
id: 022
task: lib/push-gas.sh の作成（GAS push/deploy 自動化）
date: 2026-02-24
status: 完了
phase: 5
depends_on: []
blocks: [027, 028, 032]
parallel_group: A
---

## タスク概要

ccskill-spreadsheet の `lib/push-gas.sh` を移植し、Gmail 用に調整する。
`push_gas()` と `deploy_gas()` の2関数を提供し、GAS コードの push とデプロイを自動化する。

## 背景

現状は `clasp push --force` を各スクリプト内で直接実行しており、
さらにデプロイは手動（GAS エディタ操作）が必要。
マスターアーキテクチャでは temp ディレクトリで合成 → push → deploy を自動化する。

## 作成ファイル

### `lib/push-gas.sh`

```bash
#!/bin/bash
#
# push_gas / deploy_gas - GAS project push and deploy helpers
#
# Usage (source this file, then call):
#   source "$CCSKILL_GMAIL_DIR/lib/push-gas.sh"
#   push_gas "$GAS_DIR" "$CCSKILL_GMAIL_DIR"
#   deploy_gas "$GAS_DIR" "$DEPLOYMENT_ID" "description" "$RESULT_FILE"
#

push_gas() {
    local GAS_DIR="$1"
    local MASTER_DIR="$2"

    if [ -z "$GAS_DIR" ] || [ -z "$MASTER_DIR" ]; then
        echo "Error: push_gas requires GAS_DIR and MASTER_DIR arguments"
        return 1
    fi

    if [ ! -d "$GAS_DIR" ]; then
        echo "Error: GAS_DIR does not exist: $GAS_DIR"
        return 1
    fi

    if [ ! -d "$MASTER_DIR/gas-template" ]; then
        echo "Error: gas-template not found in $MASTER_DIR"
        return 1
    fi

    # Create temp directory with cleanup
    local TMPDIR
    TMPDIR=$(mktemp -d)
    trap "rm -rf '$TMPDIR'" RETURN

    # 1. Copy master gas-template files
    /bin/cp "$MASTER_DIR/gas-template/appsscript.json" "$TMPDIR/"
    /bin/cp "$MASTER_DIR/gas-template/main.js" "$TMPDIR/"
    /bin/cp -r "$MASTER_DIR/gas-template/handlers" "$TMPDIR/"
    /bin/cp -r "$MASTER_DIR/gas-template/utils" "$TMPDIR/"

    # 2. Copy .claspignore
    if [ -f "$MASTER_DIR/gas-template/.claspignore" ]; then
        /bin/cp "$MASTER_DIR/gas-template/.claspignore" "$TMPDIR/"
    fi

    # 3. Copy project's config.js
    if [ -f "$GAS_DIR/config.js" ]; then
        /bin/cp "$GAS_DIR/config.js" "$TMPDIR/"
    else
        echo "Error: config.js not found in $GAS_DIR"
        return 1
    fi

    # 4. Copy project's .clasp.json with rootDir="."
    if [ -f "$GAS_DIR/.clasp.json" ]; then
        if command -v jq &> /dev/null; then
            jq '.rootDir = "."' "$GAS_DIR/.clasp.json" > "$TMPDIR/.clasp.json"
        else
            /bin/cp "$GAS_DIR/.clasp.json" "$TMPDIR/"
        fi
    else
        echo "Error: .clasp.json not found in $GAS_DIR"
        return 1
    fi

    # 5. clasp push from temp directory
    (cd "$TMPDIR" && clasp push --force)
}

deploy_gas() {
    local GAS_DIR="$1"
    local DEPLOYMENT_ID="${2:-}"
    local DESCRIPTION="${3:-Auto deploy}"
    local RESULT_FILE="${4:-}"

    # ... (spreadsheet 版と同一ロジック)
    # DEPLOYMENT_ID があれば clasp deploy -i で更新
    # なければ clasp deploy で新規作成
    # 出力から DEPLOY_ID を抽出して RESULT_FILE に書き込み
}
```

## spreadsheet 版との差分

変更点はほぼゼロ。以下のみ:
- コメントヘッダの Usage 例のパス名（`$CCSKILL_GMAIL_DIR`）
- ロジック自体は完全同一

## push_gas のフロー図

```
push_gas($GAS_DIR, $MASTER_DIR):
  1. mktemp -d → $TMPDIR
  2. gas-template/{appsscript.json, main.js, handlers/, utils/} → $TMPDIR/
  3. $GAS_DIR/config.js → $TMPDIR/
  4. $GAS_DIR/.clasp.json (rootDir=".") → $TMPDIR/
  5. cd $TMPDIR && clasp push --force
  6. cleanup $TMPDIR
```

## deploy_gas のフロー図

```
deploy_gas($GAS_DIR, $DEPLOYMENT_ID, $DESCRIPTION, $RESULT_FILE):
  1. mktemp -d → $TMPDIR
  2. $GAS_DIR/.clasp.json (rootDir=".") → $TMPDIR/
  3. if DEPLOYMENT_ID:
       clasp deploy -i $DEPLOYMENT_ID --description "..."
     else:
       clasp deploy --description "..."
  4. grep "Deployed" から DEPLOY_ID 抽出
  5. RESULT_FILE に書き込み（指定時）
  6. cleanup $TMPDIR
```

## テスト方法

```bash
# 開発環境で push_gas テスト
source lib/push-gas.sh
push_gas ".ccskill-gmail" "$CCSKILL_GMAIL_DIR"

# deploy テスト（新規）
deploy_gas ".ccskill-gmail" "" "Test deploy" "/tmp/deploy_result.txt"
cat /tmp/deploy_result.txt  # DEPLOYMENT_ID が出力される

# deploy テスト（更新）
DEPLOY_ID=$(cat /tmp/deploy_result.txt)
deploy_gas ".ccskill-gmail" "$DEPLOY_ID" "Update deploy"
```

## 完了条件

- [ ] `lib/push-gas.sh` が作成されている
- [ ] `push_gas` が temp ディレクトリで master + config.js を合成して push できる
- [ ] `deploy_gas` が新規デプロイと既存デプロイ更新の両方に対応
- [ ] `deploy_gas` が DEPLOYMENT_ID を抽出して返せる
- [ ] `/bin/cp` を使用（macOS の alias 対策）
