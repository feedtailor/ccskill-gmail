---
id: 084
task: Email Review Guidelines の追加（SKILL.md + examples.md）
date: 2026-04-08
status: 完了
phase: -
---

## 概要

ユーザがメール確認を依頼した際に、AI が自動通知を除外して人からのメールだけを抽出し、各メールを「内容・状態・対応案」のセットで提示するパターンを、スキルのドキュメントに組み込む。

OSS 公開後の全ユーザが同じ体験を得られるようにすることが目的。

## 背景

ftgmail プロジェクトでの実運用中に発見されたノウハウ。以下の2点が好評だった:

1. **Smart Filtering クエリ** — `newer_than:1d in:inbox -category:promotions -category:updates -category:social -from:noreply -from:no-reply -from:donotreply -from:notification -from:alert` で自動通知を除外し、人からのメールだけを抽出
2. **構造化サマリー** — 各メールを「内容・状態・対応案」のセットで提示するフォーマット

## 設計方針

スキルの Progressive Disclosure 原則に基づき、振る舞い指針と具体例を2層に分離配置する:

- **SKILL.md**（常にコンテキストに入る） — 振る舞い指針（トリガー条件 + ルール）
- **examples.md**（参照ファイル） — 具体的なクエリ例と提示フォーマット

## 変更内容

### 1. SKILL.md に「Email Review Guidelines」セクション追加

**配置**: `## Workflow Example` セクションの直後

**内容（10行程度に収める — 現在481行なので上限500行に配慮）**:

```markdown
## Email Review Guidelines

When the user asks to scan the inbox or identify emails that need a reply (e.g., "check my emails", "any emails I should reply to?", "check customer emails"), apply these patterns:

1. **Filter out automated senders** — Exclude noreply, notifications, promotions, updates, and social category emails to surface only human-sent messages. See examples.md §Smart Filtering for query patterns.
2. **Present each email as a structured summary** — For each relevant thread, report:
   - **Subject / From / Date**
   - **Content**: Brief summary of the latest message
   - **Status**: Current state (e.g., "awaiting your reply", "waiting for their response", "FYI only")
   - **Suggested action**: What to do next (e.g., "reply with acknowledgment", "no action needed", "draft reply proposed below")
```

### 2. examples.md に「Smart Filtering Patterns」セクション追加

**配置**: `## 18. Extract Unreplied Emails as Task Candidates` の直後（末尾）

**内容**:

```markdown
## 19. Smart Filtering — Surface Human Emails

When scanning the inbox for actionable emails, exclude automated senders to reduce noise:

```bash
# Filter out automated notifications and surface human-sent emails
.ccskill-gmail/api get action=search query="newer_than:1d in:inbox -category:promotions -category:updates -category:social -from:noreply -from:no-reply -from:donotreply -from:notification -from:alert" maxResults=20
```

### Customizing the Filter

```bash
# Customer emails only (if a label exists)
.ccskill-gmail/api get action=search query="newer_than:1d label:customers"

# Broader time range
.ccskill-gmail/api get action=search query="newer_than:7d in:inbox -from:noreply -from:no-reply"

# Combined with unreplied check (see §18)
# First filter with smart query, then check last sender per thread
```

### Presenting Results

After retrieving threads, fetch each thread's details and present a structured summary:

**Format:**
### 1. [Sender Name]（[Company]） — [Subject]
- **Latest**: [Who] → [Who] ([date])
- **Content**: [1-2 sentence summary]
- **Status**: [Current state]
- **Suggested action**: [What to do next]
```

### 3. README に訴求ポイントとして追記

Smart Filtering と Structured Summary を README の Features セクションに追加し、OSS としての差別化ポイントにする。

**追記イメージ**:

```markdown
- **Smart Filtering** — Surfaces human-sent emails by filtering out noise (noreply, promotions, updates, notifications)
- **Structured Summary** — Each email presented with content, status, and suggested action
```

## 検証方法

1. SKILL.md の総行数が 500行以下に収まっていること
2. スキルを発動して「未読メール確認して」と依頼し、ガイドラインに沿った振る舞いになるか確認
