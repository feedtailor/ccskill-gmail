---
id: 001
task: プロジェクト基盤の構築（ディレクトリ構造・基本ファイル）
date: 2026-01-26 09:30:00
status: 未着手
---

## タスク概要
ccskill-gmail プロジェクトの基盤を構築する。ccskill-spreadsheet の構造を参考に、ディレクトリ構造と基本ファイルを作成。

## 作成するディレクトリ・ファイル

```
ccskill-gmail/
├── gas-template/
│   ├── .claspignore
│   ├── appsscript.json          # Gmail 用スコープ設定
│   ├── config.js.template       # 設定テンプレート（Gmail は単純）
│   ├── main.js                  # エントリポイント（doGet/doPost）
│   ├── handlers/                # 空ディレクトリ（002で実装）
│   └── utils/                   # 空ディレクトリ（002で実装）
├── .claude/
│   └── skills/
│       └── ccskill-gmail/       # 空ディレクトリ（005で実装）
└── .clasp.json.template
```

## appsscript.json の内容

```json
{
  "timeZone": "Asia/Tokyo",
  "dependencies": {},
  "exceptionLogging": "STACKDRIVER",
  "runtimeVersion": "V8",
  "oauthScopes": [
    "https://www.googleapis.com/auth/gmail.readonly",
    "https://www.googleapis.com/auth/gmail.compose",
    "https://www.googleapis.com/auth/gmail.modify",
    "https://www.googleapis.com/auth/script.external_request"
  ]
}
```

## main.js の骨格

- doGet(e): status チェック用エンドポイント + 読み取り系アクション
- doPost(e): 書き込み系アクション
- ccskill-spreadsheet の main.js を参考にルーティング構造を作成
- Phase 1 のアクション: search, get_thread, get_message, list_labels, create_draft

## 依存関係
なし（最初のタスク）

## 次のタスク
002: GAS ユーティリティの実装

# 作業記録
