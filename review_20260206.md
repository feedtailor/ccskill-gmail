# ccskill-gmail レビュー (2026-02-06)

ccskill-spreadsheet プロジェクトの開発者による外部レビュー。
ccskill-spreadsheet での運用知見を踏まえ、実装方針の妥当性と改善点を報告する。

---

## 総合評価

全体として**良好**。ccskill-spreadsheet と同じアーキテクチャパターンを正確に踏襲しており、API 設計・コード品質・ドキュメントいずれも水準が高い。「送信なし・下書きのみ」の安全設計は特に優れた判断。

---

## Issue 1: "Only myself" デプロイへの移行（重要度: 高）

### 現状

現在の実装は Web App のアクセス権限を **"Anyone"（全員）** に設定する前提になっている。

- `install.sh:239` — `Who has access: 'Anyone' (REQUIRED)`
- `SKILL.md:259` — `認証: Web App は「全員がアクセス可能」で公開されている必要があります`
- `troubleshooting.md:104` — `アクセスできるユーザーが「全員」になっているか確認`

### 問題点

- Gmail は機密性が極めて高い情報を扱うため、URL 漏洩時のリスクが大きい
- "Anyone" では URL を知っている誰でもメール操作が可能になる
- 姉妹プロジェクトの ccskill-spreadsheet は既に "Only myself" + Bearer token パターンに移行済み

### 推奨対応

デプロイ設定を **"Only myself"** に変更し、全 curl 呼び出しに `Authorization: Bearer` ヘッダーを追加する。

**GAS コードの変更は不要。** ドキュメント・スクリプトの修正のみで対応可能。

#### トークン取得方法

```bash
# clasp のトークンを利用
-H "Authorization: Bearer $(jq -r '.tokens.default.access_token' ~/.clasprc.json)"
```

#### 技術的根拠

clasp トークンに `gmail.*` スコープは含まれていないが、"Only myself" のアクセス制御は GAS Web App レベルの認証（身元確認）であり、Gmail API のスコープとは無関係。実際の Gmail アクセスは `appsscript.json` の `oauthScopes` + 初回承認時の OAuth で処理される。ccskill-spreadsheet で同パターンの動作を確認済み。

#### 変更対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `install.sh` | デプロイ案内を "Only myself" に変更、ヘルスチェック curl に Authorization ヘッダー追加 |
| `.claude/skills/ccskill-gmail/SKILL.md` | 全 curl テンプレートに Authorization ヘッダー追加、制限事項更新 |
| `.claude/skills/ccskill-gmail/troubleshooting.md` | 認証エラー項目更新、「ページが見つかりません」エラー追加 |
| `.claude/skills/ccskill-gmail/examples.md` | 全 curl 例に Authorization ヘッダー追加 |
| `.claude/skills/ccskill-gmail/reference/*.md` | 全 curl 例の更新 |
| `CLAUDE.md` | curl ルールに Authorization ヘッダー記載追加 |

---

## Issue 2: 未使用バリデーション関数の整理（重要度: 低）

### 現状

`utils/validation.js` に以下の関数が定義されているが、ハンドラーコード内で一度も使用されていない:

- `isValidEmail()`
- `isValidEmailList()`
- `isValidThreadId()`
- `isValidMessageId()`
- `isValidDraftId()`
- `isPositiveInteger()`
- `isValidMaxResults()`

ハンドラー側では `requireParam()` のみが使われている。

### 推奨対応

2つの選択肢:

**A. ハンドラーに組み込む（推奨）**: `create_draft` で `isValidEmail(to)` のチェック、`get_thread` で `isValidThreadId()` のチェック等を追加し、不正な入力に対してより分かりやすいエラーメッセージを返す。

**B. 削除する**: 使わないコードは削除して GAS のコードサイズを縮小する。

---

## Issue 3: `script.external_request` スコープの不要な宣言（重要度: 低）

### 現状

`appsscript.json` に `https://www.googleapis.com/auth/script.external_request` スコープが宣言されているが、コード内に `UrlFetchApp` 等の外部リクエストを行う処理が存在しない。

### 推奨対応

不要なスコープは最小権限の原則に反するため削除する。将来的に外部リクエストが必要になった際に再追加すればよい。

```json
{
  "oauthScopes": [
    "https://www.googleapis.com/auth/gmail.readonly",
    "https://www.googleapis.com/auth/gmail.compose",
    "https://www.googleapis.com/auth/gmail.modify"
  ]
}
```

---

## Issue 4: `formatThread` の snippet フィールドの誤り（重要度: 低）

### 現状

`utils/gmail.js:20` にて:

```javascript
snippet: thread.getFirstMessageSubject(), // Actually returns snippet
```

`getFirstMessageSubject()` は名前の通り**件名**を返すメソッドであり、snippet（本文の冒頭抜粋）ではない。コメントの `// Actually returns snippet` は誤り。

### 推奨対応

- フィールド名を `snippet` → `subject` に修正するか
- 真に snippet が必要なら `firstMessage.getPlainBody().substring(0, 100)` 等で別途取得する
- もしくは `subject` フィールドと重複するため、このフィールド自体を削除する

---

## Issue 5: `.gitignore` パターンの注意（重要度: 中）

### 背景

ccskill-spreadsheet で `.ccskill-spreadsheet/` という `.gitignore` パターンが `.claude/skills/ccskill-spreadsheet/` にもマッチしてしまうバグがあった（Issue 034 で修正済み）。

### 推奨対応

`install.sh:390` で `.gitignore` に追加を推奨している `.ccskill-gmail/` パターンも同様の問題が発生する可能性がある。`/.ccskill-gmail/`（ルート限定）に修正すべき。

```
# 現状の推奨（問題あり）
.ccskill-gmail/

# 修正後の推奨
/.ccskill-gmail/
```

---

## Issue 6: `install.sh` で `cp` の代わりに `/bin/cp` を使用すべき（重要度: 低）

### 現状

`install.sh:157` にて:

```bash
cp -r "$CCSKILL_GMAIL_DIR/gas-template/"* "$GAS_DIR/src/"
```

### 推奨対応

ccskill-spreadsheet の CLAUDE.md に記載の通り、macOS では `cp` が `cp -i`（対話的確認）にエイリアスされている場合がある。`/bin/cp` を明示的に使用すべき。

---

## 参考: ccskill-spreadsheet との対応表

| 機能 | spreadsheet | gmail | 備考 |
|------|------------|-------|------|
| マルチスプレッドシート対応 | `spreadsheet` パラメータ | N/A | Gmail はアカウント単位なので不要 |
| バッチ操作 | `batch` アクション | 未実装 | 将来的に検討（複数スレッドの一括既読等） |
| config.js | SPREADSHEET_ID 保存 | 空の CONFIG | Gmail はアカウント単位なので設定不要 |
| デプロイ方式 | Only myself + Bearer | Anyone | **Issue 1 で対応すべき** |
| Authorization ヘッダー | 対応済み | 未対応 | **Issue 1 で対応すべき** |

---

## 優先順位まとめ

| 優先度 | Issue | 内容 |
|--------|-------|------|
| **高** | Issue 1 | "Only myself" デプロイへの移行 |
| 中 | Issue 5 | `.gitignore` パターン修正 |
| 低 | Issue 2 | 未使用バリデーション関数の整理 |
| 低 | Issue 3 | 不要な OAuth スコープの削除 |
| 低 | Issue 4 | snippet フィールドの誤り修正 |
| 低 | Issue 6 | `/bin/cp` の使用 |
