---
id: 008
task: create_reply_draft API の実装（返信下書き作成）
date: 2026-01-26 12:00:00
status: 未着手
---

## タスク概要

既存のスレッドに対する返信の下書きを作成する `create_reply_draft` API を実装する。

## API 仕様

**アクション**: `create_reply_draft`
**メソッド**: POST

### パラメータ

| パラメータ | 必須 | 説明 |
|-----------|------|------|
| threadId | ✓ | 返信先スレッドの ID |
| body | ✓ | 返信本文 |
| cc | | CC（カンマ区切りで複数可） |
| bcc | | BCC（カンマ区切りで複数可） |

### レスポンス例

```json
{
  "ok": true,
  "data": {
    "draftId": "r-123456789",
    "threadId": "19bf7f25b96ab637",
    "to": "original-sender@example.com",
    "subject": "Re: 元の件名",
    "message": "返信下書きを作成しました。Gmail で確認・送信してください。",
    "gmailUrl": "https://mail.google.com/mail/u/0/#drafts"
  }
}
```

## 実装内容

### 1. handlers/draft.js に追加

```javascript
function handleCreateReplyDraft(threadId, body, cc, bcc) {
  requireParam(threadId, 'threadId');
  requireParam(body, 'body');

  const thread = GmailApp.getThreadById(threadId);
  if (!thread) {
    return errorResponse(`Thread not found: ${threadId}`);
  }

  // スレッドの最後のメッセージに返信
  const messages = thread.getMessages();
  const lastMessage = messages[messages.length - 1];

  // 返信下書きを作成
  const draft = lastMessage.createDraftReply(body, {
    cc: cc,
    bcc: bcc
  });

  return successResponse({
    draftId: draft.getId(),
    threadId: threadId,
    to: lastMessage.getFrom(),
    subject: 'Re: ' + lastMessage.getSubject(),
    message: '返信下書きを作成しました。Gmail で確認・送信してください。',
    gmailUrl: 'https://mail.google.com/mail/u/0/#drafts'
  });
}
```

### 2. main.js の doPost に追加

```javascript
case 'create_reply_draft':
  return handleCreateReplyDraft(
    body.threadId,
    body.body,
    body.cc,
    body.bcc
  );
```

### 3. スキル定義の更新

- reference/draft.md に create_reply_draft を追加
- SKILL.md の API 一覧を更新

## テスト方法

```bash
# 1. スレッド ID を取得
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=search&query=is:unread&maxResults=1" | jq '.data.threads[0].id'

# 2. 返信下書きを作成
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data '{"action":"create_reply_draft","threadId":"THREAD_ID","body":"ご連絡ありがとうございます。\n\n承知いたしました。"}' \
  "$GMAIL_ENDPOINT"

# 3. Gmail で下書きを確認
```

## 依存関係

なし（Phase 1 完了済み）

# 作業記録
