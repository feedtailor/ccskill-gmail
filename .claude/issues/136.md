---
id: 136
task: "install / --dedicated 導線の退役 — グローバル4本 (account add / skill install / bind / unbind) + api への一本化を検討する"
date: 2026-06-21
status: 完了
depends_on: []
blocks: []
---

## 概要

グローバル化(#121-126)でプロジェクトとアカウントの 1:1 対応モデルを解消したにもかかわらず、`install`(central 既定)と `install --dedicated` が旧モデルの導線として残っている。これらを退役し、グローバルな4本(`account add` / `skill install` / `bind` / `unbind`)＋ `api` に一本化する方針を検討する。実装ではなく、方針の確定をゴールとする議論 issue。

## 背景・動機

`install`(central)を残すことは、解消したはずの 1:1 対応モデルの温存になる。#132 で first-run 導線からは外したが、コマンド自体は残っている。「2 つの install(`skill install` と `install`)があり、違いが名前から分からない」という UX 上の混乱も、`install` を畳めば自然に解消する。

## 確定した事実(コード根拠・調査済み)

- `bind`(bind.sh)は `binding.json` の書き込みと `setup_permissions`(権限の opt-in 追加)を行う。GAS は作らない。
- `install`(central, install.sh)は上記に加えて、スキル定義を `<project>/.claude/skills/` にコピー、`<project>/.ccskill-gmail/api` を配置、registry 登録、`.gitignore` 設定を行う。固有部分はすべて「プロジェクトにファイルを置く」旧モデルの産物。`skill install`(グローバル)＋ グローバル `api` ＋ `bind` が揃った今は冗長。
- `install --dedicated` の唯一の固有価値は「プロジェクトごとに `config.js` の権限を変える」こと。だが実環境の config.js 13 箇所(共有GAS3 + 専用GAS10)を確認した結果、すべて既定(`move_to_trash` deny のみ)で、プロジェクト別権限の運用実績はゼロ。

## 検討事項(結論は出さず論点を列挙)

- `install`(central)を `bind` のエイリアスにするか、deprecated 化するか、廃止するか。
- `install --dedicated`(専用GAS新規作成)を廃止するか。既存の専用GASはレガシー解決経路で動かし続ける。
- `uninstall` の役割再定義(install 廃止後、掃除は `unbind --purge-legacy` に寄せるか)。
- `doctor` / `help` / `README` / `security-decisions` の install 言及の整理。

## 後方互換の要件

- 既存の install 済みプロジェクト(専用GAS含む)は動作不変。
- 廃止するのは「新規に作る手段」のみ。

## 完了条件

- [ ] 上記論点について採用方針が確定し、本 issue に記録される(実装は別 issue に切り出す)

## 採用方針 (2026-06-22 確定)

論点ごとに以下を採用する。実装は #141 に切り出す。

| 論点 | 採用方針 | 補足 |
|------|----------|------|
| `install`(central) | **deprecated 化** — 警告を表示し `bind` へ委譲する（アカウント解決 + binding.json + setup_permissions のみ）。スキル定義コピー・プロジェクト api 配置・registry 登録・`.gitignore` 設定は行わない（global skill install + global api + bind で代替済みのため） | 次サイクルで完全廃止予定 |
| `install --dedicated` / `--user`(専用GAS新規作成) | **新規作成を廃止** — 実行すると退役エラーを返し、`skill install` + `bind` へ誘導する | 既存の専用GASはレガシー解決経路で動作継続 |
| `uninstall` | **当面維持** — レガシー install の掃除手段として残す。中央撤去は `uninstall --all`(#140)、レガシー掃除は `unbind --purge-legacy` と棲み分け | レガシー install 消滅後に再検討 |
| docs / doctor / help | install 言及を整理 — help の install 行に deprecated 注記、doctor の `Fix: ccskill-gmail install` 系を global 導線(`account add` / `bind` / `skill install`)へ、commands.md / security-decisions の `--dedicated` 記述を退役前提に修正 | #141 で実施 |

後方互換: 既存インストール（専用GAS含む）は動作不変。畳むのは「新規に作る手段」のみ。

確定の根拠は本 issue「確定した事実」のとおり（central install 固有部分はすべて旧モデルの産物、`--dedicated` のプロジェクト別 config.js 権限は実運用ゼロ）。2026-06-22 ユーザー判断で deprecated 化・即実装を選択。

## スコープ外 / 関連

- #132(first-run 導線)は対応済み。
- 実装: #141
- 関連: #121-126 / #126(install の再定義を見直す対象) / #132
