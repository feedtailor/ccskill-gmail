---
id: 015
task: get_unread_count API の実装（未読数取得）
date: 2026-01-26 14:00:00
status: 完了
---

## タスク概要

未読メール数を取得する `get_unread_count` API を実装する。

## API 仕様

**アクション**: `get_unread_count`
**メソッド**: GET

### パラメータ

| パラメータ | 必須 | 説明 |
|-----------|------|------|
| label | | ラベル名（省略時は INBOX） |

### レスポンス例

```json
{
  "ok": true,
  "data": {
    "label": "INBOX",
    "unreadCount": 15,
    "message": "未読メールが 15 件あります"
  }
}
```

## 実装内容

### 1. handlers/read.js に追加

```javascript
/**
 * Get unread message count
 * @param {string} labelName - Label name (optional, defaults to INBOX)
 * @returns {ContentService.TextOutput} JSON response
 */
function handleGetUnreadCount(labelName) {
  let unreadCount;
  let targetLabel = labelName || 'INBOX';

  if (!labelName || labelName.toUpperCase() === 'INBOX') {
    // 受信トレイの未読数
    unreadCount = GmailApp.getInboxUnreadCount();
    targetLabel = 'INBOX';
  } else {
    // 特定ラベルの未読数
    const label = GmailApp.getUserLabelByName(labelName);
    if (!label) {
      return errorResponse(`Label not found: ${labelName}`);
    }
    unreadCount = label.getUnreadCount();
    targetLabel = labelName;
  }

  return successResponse({
    label: targetLabel,
    unreadCount: unreadCount,
    message: `未読メールが ${unreadCount} 件あります`
  });
}
```

### 2. main.js の doGet に追加

```javascript
case 'get_unread_count':
  return handleGetUnreadCount(e.parameter.label);
```

## テスト方法

```bash
source .env

# 受信トレイの未読数
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_unread_count"

# 特定ラベルの未読数
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_unread_count&label=重要"
```

## 注意事項

- GmailApp.getInboxUnreadCount() は効率的な組み込みメソッド
- ユーザーラベルは label.getUnreadCount() を使用
- search API より軽量

## 依存関係

なし

# 作業記録

## 2026-01-26

- `handlers/read.js` に `handleGetUnreadCount` 関数を追加
- `main.js` の doGet にルーティングを追加
- INBOX の未読数と特定ラベルの未読数に対応
