---
id: 125
task: "Phase 4: bind + migrate + 履歴中央化"
date: 2026-06-10
status: 完了
phase: 4
depends_on: [124]
blocks: [126]
parallel_group: global-multiaccount
---

## 概要

既存ユーザーの正式移行経路と、ディレクトリ固定の安全機能を提供する。全体方針は #121 を参照。

## 実装内容

1. **`commands/bind.sh` (新規)** — `ccskill-gmail bind <email|label> [DIR]`: cwd (または DIR) に `.ccskill-gmail/binding.json` を書くだけの軽量バインド (GAS 作成なし、数秒)。プロジェクト permission の追記は opt-in 提示。`unbind` も対で用意
2. **`lib/api` のバインド解決** — 解決チェーンの「cwd のプロジェクトバインド」に binding.json (新形式) を追加 (旧 .ccskill-metadata.json と並ぶレガシー/新形式の両対応)
3. **`commands/migrate.sh` (新規)** — `.registry.json` の installations を email でグルーピング → 各 email につき既存デプロイの 1 つをアカウント代表として accounts.json に登録 (**再認可不要**) → 各プロジェクトに binding.json を書く (旧メタデータは残置) → 余った重複 GAS デプロイは一覧表示して手動削除を案内 (自動削除しない)
4. **履歴の中央化** — `lib/history.sh`: 記録先を `~/.ccskill-gmail/history/<email>/audit.jsonl` に変更、レコードに `account` フィールド追加。レガシープロジェクト (バインド済み) では従来位置への記録も維持 (`CCSKILL_HISTORY_DIR` 上書きは存続)。sandbox 内で書込不可なら無言スキップ (既存縮退設計 :38-47 踏襲)
5. **`commands/history.sh` の対応** — アカウント横断表示・`--account` フィルタ
6. **`doctor` 拡充** — PATH / accounts.json / binding / sandbox 書込可否 / ユーザースキル登録状態の診断

## 完了条件

- [x] `bind` したディレクトリでは `--account` 指名がない限り必ずバインド先アカウントが使われる (ユニットテストで固定。優先順位: flag > binding.json > レガシーメタデータ > default)
- [x] 既存マルチプロジェクト環境で `migrate` 実行 → 再認可なしで accounts.json 登録 + 各プロジェクトの binding 完了、従来の `.ccskill-gmail/api` も動き続ける (2026-06-11 実環境で本実行: 3 アカウント登録・9 プロジェクト bind・余剰 7 デプロイ列挙・skip 1。ftgmail で `account_source:"binding"` による共有デプロイ動作を確認)
- [x] グローバル利用時の操作が `~/.ccskill-gmail/history/<email>/` に記録され、`ccskill-gmail history` でアカウント横断表示できる (ユニットテストで固定。--account / --all、未インストール dir では中央履歴を表示)
- [x] sandbox 内実行で履歴書込が失敗しても API 操作自体は成功する (#123 の無音スキップを中央ディレクトリにも適用)

## 実装記録 (2026-06-10)

- `commands/bind.sh` 新規: `bind <email|label> [DIR] [--yes]` (binding.json に**解決済み email** を保存) / `unbind`。permission 追加は setup_permissions の opt-in 経由
- `lib/accounts.sh`: `accounts_write_binding` 追加 (bind / migrate で共用)
- `lib/api`: binding.json (SCRIPT_DIR → PWD) を明示指定の次・レガシーメタデータより前に解決。accounts.json 経由なので account/account_source を注入 (source=binding)。バインド先未登録は UNKNOWN_ACCOUNT
- 履歴中央化: accounts.json 解決時は `~/.ccskill-gmail/history/<email>/audit.jsonl` に記録 + レコードに `account` フィールド。レガシーメタデータは従来どおりプロジェクト配下。事前設定の CCSKILL_HISTORY_DIR を尊重
- `lib/history.sh`: 複数ディレクトリ対応 (CCSKILL_HISTORY_DIRS)、時系列ソート、human 表示に @account
- `commands/history.sh`: `--account` / `--all`。未インストール dir では中央履歴を表示 (従来はエラーだった)。clear は project または --account 単位
- `commands/migrate.sh` 新規: レジストリ記載順の最初を代表に採用。既存 accounts.json エントリは不可侵。--dry-run / --yes。テスト用 CCSKILL_GMAIL_REGISTRY_FILE
- `commands/doctor.sh`: ユーザースキル登録状態、binding 表示、bind 専用ディレクトリ (フル install なし) の正常扱い
- テスト: test-bind-cmd.sh (10) / test-history-central.sh (6) / test-migrate.sh (6) 新規。全 101 件 green。ftgmail 回帰 OK
- マージ後フォロー: 中央履歴ディレクトリの mkdir 失敗 (sandbox) が stderr ノイズになる問題を無音スキップに修正 (lib/history.sh `_ccskill_history_init_dir`)

### スコープ調整

- doctor での「sandbox 書込可否」診断は見送り: doctor は Claude Code の sandbox 外 (ユーザーシェル) で実行されるため、sandbox 下の挙動をその場で検証できない。縮退設計 (#123) とドキュメント記載で代替
- 実環境での migrate 本実行は、バインド先が「アカウント共有デプロイ」に切り替わる実害のない構成変更だが、プロジェクト個別の config.js 運用をしている場合に影響するためユーザー判断とする (#121 のトレードオフ参照)

## 関連

- #121: 全体方針 / #124: 前 Phase / #126: 次 Phase
