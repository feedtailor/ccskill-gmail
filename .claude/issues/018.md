---
id: 018
task: Phase 3 動作確認・テスト
date: 2026-01-26 14:00:00
status: 未着手
---

## タスク概要

Phase 3 の全機能をテストし、動作確認を行う。

## テスト項目

### 1. archive

- [ ] 正常系: スレッドをアーカイブ
- [ ] エラー系: 存在しない threadId

### 2. move_to_trash

- [ ] 正常系: スレッドをゴミ箱に移動
- [ ] エラー系: 存在しない threadId

### 3. get_unread_count

- [ ] 正常系: 受信トレイの未読数取得
- [ ] 正常系: 特定ラベルの未読数取得
- [ ] エラー系: 存在しないラベル

### 4. update_draft

- [ ] 正常系: 下書きの件名を更新
- [ ] 正常系: 下書きの本文を更新
- [ ] エラー系: 存在しない draftId

### 5. delete_draft

- [ ] 正常系: 下書きを削除
- [ ] エラー系: 存在しない draftId

### 6. 完全ワークフローテスト

```bash
source .env

# 1. 未読数を確認
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_unread_count" | jq .

# 2. 未読メールを検索
THREAD_ID=$(curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=search&query=is:unread&maxResults=1" | jq -r '.data.threads[0].id')

# 3. スレッドの内容を確認
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_thread&threadId=$THREAD_ID" | jq '.data.subject, .data.messages[-1].body'

# 4. 返信下書きを作成
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data "{\"action\":\"create_reply_draft\",\"threadId\":\"$THREAD_ID\",\"body\":\"承知いたしました。\"}" \
  "$GMAIL_ENDPOINT"

# 5. 既読にする
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data "{\"action\":\"mark_read\",\"threadId\":\"$THREAD_ID\"}" \
  "$GMAIL_ENDPOINT"

# 6. ラベルを追加
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data "{\"action\":\"add_label\",\"threadId\":\"$THREAD_ID\",\"label\":\"対応済\"}" \
  "$GMAIL_ENDPOINT"

# 7. アーカイブ
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data "{\"action\":\"archive\",\"threadId\":\"$THREAD_ID\"}" \
  "$GMAIL_ENDPOINT"

# 8. 未読数を再確認（減っているはず）
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_unread_count" | jq .
```

## 成功基準

- 全 API が正常にレスポンスを返す
- エラー時に適切なエラーメッセージが返る
- Gmail UI で変更が反映されている
- 完全ワークフローが一貫して動作する

## 依存関係

013, 014, 015, 016, 017 の完了後

# 作業記録
