---
id: 005
task: スキル定義の作成（SKILL.md, reference/）
date: 2026-01-26 09:30:00
status: 完了
---

## タスク概要
Claude Code 用のスキル定義ファイルを作成する。ccskill-spreadsheet のスキル定義を参考に、Gmail 用にカスタマイズ。

## 作成するファイル

```
.claude/skills/ccskill-gmail/
├── SKILL.md              # メインスキル定義
├── troubleshooting.md    # トラブルシューティング
├── examples.md           # ワークフロー例
└── reference/
    ├── index.md          # API 概要
    ├── search.md         # search API
    ├── read.md           # get_thread, get_message, list_labels
    └── draft.md          # create_draft
```

## SKILL.md の構成

1. 概要（Gmail 操作スキル）
2. 環境設定（GMAIL_ENDPOINT）
3. curl 実行時の必須ルール
   - GET/POST の使い分け
   - --max-time 60
   - -L オプション
4. API 一覧（Phase 1）
5. Gmail 検索構文の例

## curl コマンドテンプレート

```bash
# 環境変数の読み込み
source .env

# GET: メール検索
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=search&query=is:unread&maxResults=10"

# GET: スレッド取得
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_thread&threadId=THREAD_ID"

# POST: 下書き作成
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data '{"action":"create_draft","to":"example@gmail.com","subject":"件名","body":"本文"}' \
  "$GMAIL_ENDPOINT"
```

## examples.md の内容

1. 未読メールの確認ワークフロー
2. 特定の送信者からのメール検索
3. 返信下書きの作成（Phase 2 で実装、ここでは get_thread → create_draft の組み合わせ）

## troubleshooting.md の内容

1. GAS コールドスタートでのタイムアウト
2. Gmail スコープの承認エラー
3. 検索クエリのエスケープ

## 依存関係
003: GAS ハンドラの実装（API 仕様が確定）

## 次のタスク
006: 動作確認・テスト

# 作業記録
## 2026-01-26 10:15:00
スキル定義を作成:
- SKILL.md: メインスキル定義（API一覧、curlテンプレート、Gmail検索構文）
- reference/index.md: APIリファレンス索引
- reference/search.md: search API 詳細
- reference/read.md: get_thread, get_message, list_labels 詳細
- reference/draft.md: create_draft 詳細
- troubleshooting.md: トラブルシューティング
- examples.md: ワークフロー例
