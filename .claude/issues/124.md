---
id: 124
task: "Phase 3: ユーザースキル登録 + ドキュメント全面改訂"
date: 2026-06-10
status: 完了
phase: 3
depends_on: [123]
blocks: [125]
parallel_group: global-multiaccount
---

## 概要

スキルを `~/.claude/skills/ccskill-gmail` に登録できるようにし、ドキュメントをグローバル表記 (`ccskill-gmail api`) に全面改訂する。この Phase の完了で要求 (A)(B) がプロンプトから使える状態になる — リリースの主役。全体方針は #121 を参照。

## 実装内容

1. **`commands/skill.sh` (新規)** — `ccskill-gmail skill install`: `~/.claude/skills/ccskill-gmail` → マスター `.claude/skills/ccskill-gmail/` への symlink 作成。symlink 不可環境用 `--copy` (この場合のみ update で再コピー)。`skill uninstall` も対で用意。`~/.claude/settings.json` には触れない (permission は案内表示のみ、#121 の不可侵方針)
2. **ドキュメント表記統一 (約 170 箇所、機械的)** — SKILL.md (23) / SKILL.ja.md (同等) / examples.md (65) / reference/* (67) / troubleshooting.md の `.ccskill-gmail/api` → `ccskill-gmail api`。`.ccskill-gmail/tmp/` 表記は維持 (cwd 相対のまま、api が実行時に mkdir)
3. **SKILL.md にアカウント行動規範を追加** (#121 の誤アカウント対策):
   - 結果報告で必ず対象アカウントを明示する
   - ユーザーがメールアドレス/ラベルを指名した時のみ `--account` を付与する
   - 曖昧 (複数登録 + バインドなし + 不明瞭な指名) なら実行前にユーザーへ確認する
   - post は直前の get と同じ `--account` を引き継ぐ (会話途中の暗黙切替禁止)
4. **frontmatter 更新** — description をグローバル前提に書き直し (「cd したプロジェクトに紐づく」記述の更新)。`allowed-tools` の permission パターンは #122 の検証結果に従う
5. **lib/permissions.sh 拡張** — `Bash(ccskill-gmail api:*)` 系パターンを追加 (レガシーパターンは残置)

## 完了条件

- [x] `skill install` 後、install していないプロジェクトの新規セッションで「メールを確認して」系の依頼が `ccskill-gmail api` 経由で完結する (クリーン環境テスト)
- [x] 「<email> のほうで確認して」で `--account` が付与され、指名なしでは付与されない (クリーン環境テスト)
- [x] 結果報告にアカウントが明示される
- [x] ドキュメントに `.ccskill-gmail/api` の呼び出し表記が残っていない (テストで固定。意図的なレガシー注記 2 行のみ許可)
- [x] レガシー install 済みプロジェクトでの動作が壊れていない (グローバル表記がバインド解決で動作。※バインド先とデフォルトが同一アカウントのため由来の区別は未観測)

### クリーン環境 LLM テストの記録 (2026-06-10、3 セッション)

1. **未インストールの /tmp で「メールを確認して。直近3時間で未読は何件?」** — `Skill(ccskill-gmail)` 発火 → `ccskill-gmail api get action=search query="is:unread newer_than:3h"` を自力構築 → 報告冒頭で「oishi@feedtailor.jp の…」とアカウント明示 ✅。指名なしのため `--account` なし ✅。未読数の明示的質問なので is:unread 使用は正当 ✅
2. **「main アカウントで受信トレイを確認して」** — `account list` で main = email を確認してから `--account oishi@feedtailor.jp` を付与 ✅。報告で「oishi@feedtailor.jp（main アカウント）」 ✅。レビュー系依頼に triage 基本クエリ使用 (is:unread 不使用) ✅
3. **レガシー install 済みプロジェクトで「メールの確認をして」** — ユーザースキル (新ドキュメント) が適用され、triage.md を自発参照 → 基本クエリ → 全スレッド get_thread → reply-now / 要内部アクション / archive に分類 ✅。グローバル表記がバインド環境でも正常動作 ✅

副次的観察: 3 セッションとも標準 Gmail コネクタ (MCP) の先行発火が**発生しなかった** (#118 では 2 連続発生)。description のグローバル化が寄与した可能性があるが、観測数が少ないため断定しない。

## 実装記録 (2026-06-10)

- `commands/skill.sh` 新規: `skill install` (~/.claude/skills/ccskill-gmail → マスターへの symlink。`--copy` で実体コピー)、`skill uninstall [--yes]`。当スキル以外の実体ディレクトリは壊さないガード付き。`~/.claude/settings.json` には一切触れない (テストで固定)
- `lib/permissions.sh`: `Bash(ccskill-gmail api *)` パターンを追加 (レガシーパターン残置)
- ドキュメント改訂 (17 ファイル): `.ccskill-gmail/api` → `ccskill-gmail api` 全置換 + 文脈修正
  - SKILL.md / SKILL.ja.md: frontmatter description グローバル化、Overview のアカウント解決説明、**Account Selection — Critical Rules セクション新設** (報告で必ずアカウント明示 / 指名時のみ --account / 曖昧なら確認 / post は直前 get と同じ --account)、Response Handling に account/account_source、サブコマンド 6 つ (whoami 追加)、レガシー注記
  - examples.md: 前提・比較表・スクリプト生成の cwd 依存記述を更新
  - reference/scripting.md: 「任意ディレクトリで実行可。スクリプトでは --account / CCSKILL_GMAIL_ACCOUNT で固定」に全面改訂
  - reference/index.md: アカウント解決の説明 + whoami
  - troubleshooting.md: NO_ACCOUNT / UNKNOWN_ACCOUNT の項目追加、レガシー endpoint ファイル参照を doctor / whoami に置換
- frontmatter `allowed-tools: Bash, Write` は維持 (パターン限定にすると `| jq` 併用コマンドがプロンプト対象になり UX が落ちるため。#122 検証 (3) で仕様確認済み)
- テスト: `tests/lib/test-skill-cmd.sh` (7)、`tests/lib/test-docs-global-syntax.sh` (5) 新規。全 79 件 green
- 実機: `skill install` 実行済み (symlink)。symlink はマスター作業ツリーを指すため、ブランチ切替で内容が変わる点に注意 (マージ後に安定)

## 備考

クリーン環境での LLM 挙動テストは #118 で確立した手順 (新規セッション・シナリオ別に分離・API 呼び出しを直接観察) に倣う。テスト時は標準 Gmail コネクタの先行発火に注意 (#118 で 2 連続再現)。

## 関連

- #121: 全体方針 / #123: 前 Phase / #125: 次 Phase
- #117, #118: 「行動規範は SKILL.md inline 必須」の先行知見とクリーン環境テスト手順
