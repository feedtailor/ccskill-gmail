---
id: 024
task: lib/permissions.sh の作成（Claude Code パーミッション自動設定）
date: 2026-02-24
status: 未着手
phase: 5
depends_on: []
blocks: [027, 028]
parallel_group: A
---

## タスク概要

ccskill-spreadsheet の `lib/permissions.sh` を移植。
`setup_permissions()` 関数で `.claude/settings.local.json` に
`ccskill-get` / `ccskill-post` の許可パターンを自動追加する。

## 背景

新しい API ラッパー（`ccskill-get` / `ccskill-post`）導入後、
Claude Code が毎回 Bash 実行の許可を求めてくるのを防ぐ。
install / update 時にオプトインで設定。

## 作成ファイル

### `lib/permissions.sh`

ccskill-spreadsheet の `lib/permissions.sh` をそのまま流用可能。

```bash
#!/bin/bash
#
# Gmail Skill - Permission Helper
#
# Claude Code の .claude/settings.local.json に ccskill-get / ccskill-post の
# 許可パターンを追加するヘルパー関数。
#
# Usage:
#   source lib/permissions.sh
#   setup_permissions /path/to/target/project [--yes]
#

setup_permissions() {
    local target_dir="$1"
    local auto_yes="${2:-}"
    local settings_file="$target_dir/.claude/settings.local.json"
    local patterns=("Bash(ccskill-get:*)" "Bash(ccskill-post:*)")
    local missing_patterns=()

    # jq が必要
    if ! command -v jq &> /dev/null; then
        echo -e "${YELLOW:-}Note: jq not found. Skipping permission setup.${NC:-}"
        echo "  To manually allow ccskill commands, add the following to $settings_file:"
        echo '    "Bash(ccskill-get:*)", "Bash(ccskill-post:*)"'
        return 0
    fi

    # 既存設定をチェックして未追加のパターンを特定
    if [ -f "$settings_file" ]; then
        for pattern in "${patterns[@]}"; do
            if ! jq -e --arg p "$pattern" \
                '.permissions.allow // [] | index($p)' \
                "$settings_file" > /dev/null 2>&1; then
                missing_patterns+=("$pattern")
            fi
        done
    else
        missing_patterns=("${patterns[@]}")
    fi

    # 全パターンが既に設定済み
    if [ ${#missing_patterns[@]} -eq 0 ]; then
        echo -e "${GREEN:-}✓ Permission patterns already configured${NC:-}"
        return 0
    fi

    # opt-in 確認
    echo ""
    echo "Claude Code の許可設定に以下のパターンを追加できます："
    for pattern in "${missing_patterns[@]}"; do
        echo "  - $pattern"
    done
    echo ""
    echo "これにより、Skill 使用時の確認プロンプトが軽減されます。"
    echo "設定先: $settings_file"
    echo ""

    if [ "$auto_yes" != "--yes" ] && [ "$auto_yes" != "-y" ]; then
        read -p "許可パターンを追加しますか？ (y/N): " -n 1 -r
        echo
        if [[ ! $REPLY =~ ^[Yy]$ ]]; then
            echo "スキップしました。"
            return 0
        fi
    fi

    # settings.local.json の作成または更新
    if [ ! -f "$settings_file" ]; then
        mkdir -p "$(dirname "$settings_file")"
        local allow_json="[]"
        for pattern in "${missing_patterns[@]}"; do
            allow_json=$(echo "$allow_json" | jq --arg p "$pattern" '. + [$p]')
        done
        jq -n --argjson allow "$allow_json" \
            '{"permissions": {"allow": $allow}}' > "$settings_file"
    else
        local tmp; tmp=$(mktemp)
        local current; current=$(cat "$settings_file")
        for pattern in "${missing_patterns[@]}"; do
            current=$(echo "$current" | jq --arg p "$pattern" '
                .permissions.allow = ((.permissions.allow // []) + [$p])
            ')
        done
        echo "$current" > "$tmp"
        /bin/mv "$tmp" "$settings_file"
    fi

    echo -e "${GREEN:-}✓ Permission patterns added (${#missing_patterns[@]} patterns)${NC:-}"
    return 0
}
```

## spreadsheet 版との差分

変更点: **コメントヘッダーのみ**。ロジックは完全同一。
パーミッションパターン `Bash(ccskill-get:*)` / `Bash(ccskill-post:*)` は
プロジェクト非依存なので同じ。

## 完了条件

- [ ] `lib/permissions.sh` が作成されている
- [ ] `setup_permissions` が `.claude/settings.local.json` を正しく更新する
- [ ] 既存パターンがある場合はスキップする
- [ ] `--yes` フラグで確認なしに追加できる
- [ ] jq 未インストール時にもエラーにならない
