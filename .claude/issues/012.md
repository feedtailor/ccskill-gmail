---
id: 012
task: Phase 2 動作確認・テスト
date: 2026-01-26 12:00:00
status: 完了
---

## タスク概要

Phase 2 の全機能をテストし、動作確認を行う。

## テスト項目

### 1. create_reply_draft

- [ ] 正常系: スレッドへの返信下書き作成
- [ ] 正常系: CC/BCC 付き返信
- [ ] エラー系: 存在しない threadId

### 2. mark_read / mark_unread

- [ ] 正常系: スレッドを既読に
- [ ] 正常系: スレッドを未読に
- [ ] 正常系: メッセージを既読に
- [ ] 正常系: メッセージを未読に
- [ ] エラー系: threadId も messageId もない

### 3. add_label / remove_label

- [ ] 正常系: 既存ラベルを追加
- [ ] 正常系: 新規ラベルを追加（自動作成）
- [ ] 正常系: ラベルを削除
- [ ] エラー系: 存在しないラベルを削除

### 4. ワークフローテスト

```bash
source .env

# 1. 未読メールを検索
THREAD_ID=$(curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=search&query=is:unread&maxResults=1" | jq -r '.data.threads[0].id')

# 2. スレッドの内容を確認
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_thread&threadId=$THREAD_ID" | jq '.data.subject, .data.messages[-1].body'

# 3. 返信下書きを作成
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data "{\"action\":\"create_reply_draft\",\"threadId\":\"$THREAD_ID\",\"body\":\"承知いたしました。\"}" \
  "$GMAIL_ENDPOINT"

# 4. 既読にする
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data "{\"action\":\"mark_read\",\"threadId\":\"$THREAD_ID\"}" \
  "$GMAIL_ENDPOINT"

# 5. ラベルを追加
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data "{\"action\":\"add_label\",\"threadId\":\"$THREAD_ID\",\"label\":\"対応済\"}" \
  "$GMAIL_ENDPOINT"
```

## 成功基準

- 全 API が正常にレスポンスを返す
- エラー時に適切なエラーメッセージが返る
- Gmail UI で変更が反映されている

## 依存関係

008, 009, 010, 011 の完了後

# 作業記録

## 2026-01-26

### テスト結果

| API | 正常系 | エラー系 |
|-----|--------|---------|
| create_reply_draft | ✓ | ✓ (無効なthreadId) |
| mark_read | ✓ | ✓ (ID未指定) |
| mark_unread | ✓ | - |
| add_label | ✓ | - |
| remove_label | ✓ | ✓ (存在しないラベル) |

全テスト成功
