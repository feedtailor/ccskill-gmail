---
id: 144
task: "退役済み 'ccskill-gmail install' を案内するエラー/ヒントを現行導線へ是正（#141 取りこぼし）"
date: 2026-06-24
status: 完了
depends_on: [141]
blocks: []
---

## 概要

#141 で `install` を退役（deprecated 化 + 専用GAS新規作成の廃止）したが、認証切れ・未設定時のユーザー向けエラー/ヒントが、いまや存在しない `ccskill-gmail install` を案内したまま残っている。復旧導線が機能しないため、現行の global 導線（`setup` / `account add` / `bind`）へ是正する。

セキュリティではなく正確性の不具合。codex-reviewer による外部レビュー（2026-06-24）の検証中に発見。

## 該当箇所と是正方針

doctor.sh が #141 で確立したマッピングに揃える（認証系→`account add`/`setup`、master 破損→`setup`、プロジェクト未設定→`bind`）。

| 箇所 | 現状 | 是正後 |
|------|------|--------|
| `lib/auth.sh:22` | clasprc なし → `install` | `ccskill-gmail setup`（未セットアップ） |
| `lib/auth.sh:37` | アカウントの access_token なし → `install` | `ccskill-gmail account add`（当該アカウント未登録） |
| `lib/auth.sh:57` | refresh 用クレデンシャルなし → `install` | `ccskill-gmail account add` |
| `lib/auth.sh:75` | refresh 失敗 → `install` | `ccskill-gmail account add`（再認証） |
| `lib/api:48` | master ディレクトリなし → `install` | `ccskill-gmail setup` |
| `lib/api:184` | master に auth.sh なし → `install` | `ccskill-gmail setup` |
| `lib/provision.sh:37` | RETRY_HINT 既定値 `install` | `ccskill-gmail account update`（呼び出し元 account.sh は既に上書き済。既定値の保険を是正） |
| `commands/info.sh:73` | プロジェクト未設定 → `install` | `ccskill-gmail bind <email|label>` |
| `commands/apply-config.sh:42` | `.ccskill-gmail/` なし → `install` | `ccskill-gmail bind <email|label>` |
| `commands/update.sh:81` | 未設定 → `install` | `ccskill-gmail bind <email|label>`（プロジェクト単位の更新導線） |

対象外（正当な `install` 言及・退役後も残す）:
- `commands/install.sh`（退役本体・deprecated 通知）
- `commands/register.sh:44` / `commands/skill.sh:112` / `commands/info.sh:11,71`（"installation" の説明的記述で、退役コマンドの案内ではない）

## テスト（TDD・回帰防止）

`tests/lib/test-install-deprecation.sh` にガードケースを追加:
- `lib/` と `commands/` のユーザー向け復旧ヒントに `ccskill-gmail install` が現れないことを検証（install.sh の deprecated 通知と説明的記述は除外）。

## 完了条件

- [x] 上記9箇所を現行導線へ是正
- [x] ガードテスト追加で退役コマンド案内の再混入を防止
- [x] 既存テストが壊れない

## 実装記録 (2026-06-24)

完了。TDD（Red→Green）で実装。

- ガードテスト `tests/lib/test-install-deprecation.sh` に (9) を追加。`lib/` と `commands/` を `ccskill-gmail install([^a-zA-Z]|$)` で走査し、退役 install の復旧ヒントが残っていれば失敗する（install.sh 本体と `installation`/`installed` の説明的記述は除外）。修正前は10行ヒットで Red を確認。
- 9箇所を是正: `lib/auth.sh`（clasprc 不在→`setup`、アカウント未登録/再認証→`account add` ×3）、`lib/api`（master/auth 破損→`setup` ×2）、`lib/provision.sh`（RETRY_HINT 既定→`account update`）、`commands/info.sh` `commands/apply-config.sh` `commands/update.sh`（プロジェクト未設定→`bind <email|label>`）。
- 回帰確認: `tests/lib/` 全21本グリーン。

## 関連

- #141 install 退役
- codex-reviewer 外部レビュー（2026-06-24）の検証過程で発見
