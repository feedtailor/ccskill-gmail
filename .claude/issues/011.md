---
id: 011
task: Phase 2 スキル定義の更新
date: 2026-01-26 12:00:00
status: 未着手
---

## タスク概要

Phase 2 で追加した API のスキル定義（SKILL.md, reference/）を更新する。

## 更新内容

### 1. SKILL.md の API 一覧に追加

```markdown
### 書き込み (POST) - Phase 2

| アクション | 説明 | パラメータ |
|-----------|------|-----------|
| create_reply_draft | 返信下書き作成 | `threadId`, `body`, `cc`, `bcc` |
| mark_read | 既読にする | `threadId` or `messageId` |
| mark_unread | 未読にする | `threadId` or `messageId` |
| add_label | ラベル追加 | `threadId`, `label` |
| remove_label | ラベル削除 | `threadId`, `label` |
```

### 2. reference/ に追加

- reference/draft.md に `create_reply_draft` を追記
- reference/status.md を新規作成（mark_read, mark_unread）
- reference/label.md を新規作成（add_label, remove_label）
- reference/index.md を更新

### 3. examples.md に追加

Phase 2 API を使ったワークフロー例：
- 未読メールを確認→返信下書き作成→既読にする
- メールにラベルを付けて整理する

## 依存関係

008, 009, 010 の完了後

# 作業記録
