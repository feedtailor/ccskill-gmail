---
id: 006
task: 動作確認・テスト（Phase 1 完了）
date: 2026-01-26 09:30:00
status: 完了
---

## タスク概要
Phase 1 の全機能をテストし、動作確認を行う。

## テスト項目

### 1. install.sh のテスト

```bash
# テスト用ディレクトリで実行
mkdir -p /tmp/test-gmail-skill
cd /tmp/test-gmail-skill
$CCSKILL_GMAIL_DIR/install.sh test-project
```

- [ ] スキル定義がコピーされる
- [ ] GAS コードがコピーされる
- [ ] clasp create が成功する
- [ ] clasp push が成功する

### 2. GAS デプロイのテスト

- [ ] 手動デプロイが成功する
- [ ] OAuth スコープの承認が正常に完了する

### 3. API テスト

```bash
source .env

# ヘルスチェック
curl -sL --max-time 60 "$GMAIL_ENDPOINT"

# search: 未読メール
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=search&query=is:unread&maxResults=5"

# list_labels
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=list_labels"

# get_thread（search で取得した threadId を使用）
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_thread&threadId=THREAD_ID"

# get_message（get_thread で取得した messageId を使用）
curl -sL --max-time 60 "$GMAIL_ENDPOINT?action=get_message&messageId=MESSAGE_ID"

# create_draft
curl -sL --max-time 60 \
  -H "Content-Type: application/json" \
  --data '{"action":"create_draft","to":"test@example.com","subject":"テスト","body":"テスト本文"}' \
  "$GMAIL_ENDPOINT"
```

### 4. エラーハンドリングのテスト

- [ ] 存在しない threadId でのエラー
- [ ] 必須パラメータ欠落でのエラー
- [ ] 不正な action でのエラー

### 5. スキル定義のテスト

- [ ] Claude Code でスキルが認識される
- [ ] SKILL.md の curl コマンド例が動作する

## 成功基準

- 全 API が正常にレスポンスを返す
- エラー時に適切なエラーメッセージが返る
- Claude Code からスキルとして利用可能

## 依存関係
001-005: 全タスクの完了

## Phase 1 完了後
- README.md の作成
- update.sh / uninstall.sh の作成（必要に応じて）
- Phase 2 の issue 作成

# 作業記録
## 2026-01-26 10:30:00
Phase 1 全機能の動作確認完了:

### テスト結果
- [x] install.sh: GASプロジェクト作成・push成功
- [x] 手動デプロイ: OAuth承認成功
- [x] ヘルスチェック: ✅ `{"ok":true,"data":{"status":"ok","message":"Gmail Skill is running"}}`
- [x] list_labels: ✅ 59ラベル取得
- [x] search: ✅ 未読メール検索成功（is:unread, is:important等）
- [x] get_thread: ✅ メッセージ本文・HTML取得成功
- [x] create_draft: ✅ 下書き作成成功（Gmail確認可能）

### テスト環境
- エンドポイント: /tmp/test-gmail-skill/.env に保存
- GASプロジェクト: https://script.google.com/d/1GfL8-yId9hUcpa7aHy_0-AXadXVKEzxhMVzfVIyCrKDR2Zbfr-REmf18/edit
