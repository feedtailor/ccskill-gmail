---
id: 087
task: security-decisions.md の英語化・表現改善・追記
date: 2026-04-08
status: 未着手
phase: -
---

## 概要

`docs/security-decisions.md` を英語版を本番として整備し、日本語版を `.ja.md` として併設する。
あわせて表現の改善と、#049 以降に追加された機能に関するセキュリティ判断を追記する。

## 変更内容

### 1. 英語化・多言語対応
- `docs/security-decisions.md` → 英語版（本番）
- `docs/security-decisions.ja.md` → 日本語版を新規作成
- 既存パターン（README.ja.md, SKILL.ja.md）に準拠

### 2. 表現改善
- 「不採用項目」→ セキュリティレビューで指摘されたが意図的に不採用とした経緯を明記
- テーブルの「項目」列 → 「指摘項目名」相当の表現に変更
- 読者が「なぜこの指摘を受けたのか」「なぜ不採用としたのか」を理解しやすくする

### 3. 「採用した対策」セクションを包括的一覧に拡充
現在は箇条書き6件+「issue #049 を参照」だが、全セキュリティ対策を issue 番号付きで一覧化する。

対象となる対策と issue:

| 対策 | issue |
|------|-------|
| GAS Web App を `MYSELF` アクセスに制限 | #026 |
| `config.js` の allow/deny パーミッション機構、`move_to_trash` デフォルト deny | #041 |
| `formatMessage` から `htmlBody` を除外（HTML 攻撃面の排除） | #049 |
| ゼロ幅文字の自動除去（`stripInvisibleChars`） | #049 |
| メール本文の境界マーカー（`EMAIL CONTENT START/END`） | #049 |
| SKILL.md への間接プロンプトインジェクション注意書き | #049 |
| `escapeHtml_` のシングルクォートエスケープ追加 | #049 |
| `.gitignore` 自動設定（install 時） | #049 |
| ローカル操作監査ログ（`history.sh`） | #050 |
| Subject フィールドへの `stripInvisibleChars` 適用拡大 | #061 |
| POST用JSON一時ファイルを `.ccskill-gmail/tmp/` に移動（`/tmp/` 汚染回避） | #085 |
| 永久削除 API を意図的に不提供 | （設計原則） |
| Bulk 操作に `isActionAllowed` パーミッションチェック適用 | #057 |
| Bulk 操作に `dryRun` オプション提供 | #057 |

### 4. 不採用項目の追記
- Bulk 操作の件数上限（不採用理由: GAS の6分実行制限が自然な上限として機能）
- その他、#060 で議論された `installed_from` 関連の判断は既に記載済み
